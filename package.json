{
  "name": "browserify-babel-async-await-demo",
  "description": "A minimal demo of async/await working in a browser in April 2018.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "http-server",
    "prestart": "npm run build",
    "build": "browserify index.js -t babelify -o dist.js"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-plugin-transform-async-to-generator": "^6.24.1",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.6.1",
    "babelify": "^8.0.0",
    "browserify": "^16.1.1",
    "http-server": "^0.11.1"
  }
}
